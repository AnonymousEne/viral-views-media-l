specVersion: "v1"
serviceId: "functions"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "y-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
